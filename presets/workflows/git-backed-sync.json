{
  "name": "workflows/git-backed-sync",
  "version": "1.0.0",
  "description": "Git-backed CarryCtx state sync: orphan snapshot branch, pre-push hook automation, and cloner onboarding using export/import",
  "author": "CarryCtx",
  "license": "MIT",
  "carryctx": {
    "engine": ">=0.8.2"
  },
  "workflows": [
    {
      "source": "presets/workflows/git-backed-sync.md",
      "target": "workflows/git-backed-sync.md",
      "kind": "workflow"
    }
  ],
  "permissions": {
    "requires_filesystem": true,
    "requires_network": false,
    "requires_env": []
  }
}
