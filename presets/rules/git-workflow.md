# Git Workflow Rules

- Write atomic, focused commits.
- Use Conventional Commits format (e.g., `feat: ...`, `fix: ...`, `docs: ...`).
- Keep Pull Requests small and single-purpose.
- Rebase instead of merge to keep history linear when updating feature branches.
